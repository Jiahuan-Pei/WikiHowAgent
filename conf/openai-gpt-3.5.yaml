llm:
  model_type: "openai"  # or "ollama"
  model: "gpt-3.5-turbo"  # Specify the model name
  temperature: 0.7  # Set the temperature for the model

embeddings:
  type: "openai"  # or "ollama"
  model: "text-embedding-ada-002"  # Specify the embedding model name