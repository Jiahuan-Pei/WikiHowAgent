llm:
  model_type: "openai"  # or "ollama"
  model: "gpt-4-nlp"  # Specify the model name
  temperature: 0.0  # Set the temperature for the model
  max_tokens: None
  timeout: None
  max_retries: 2
  api_key: "YOUR_API_KEY"
  base_url: "YOUR_BASE_URL"

params:
  root_doc_dir: data/wikihow
  index_file: data/faiss_vector_store
  rubric_file: data/evaluation_rubrics.json
  k: 1 # Top k docs
  verbose: False